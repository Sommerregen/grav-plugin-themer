form:
  validation: loose

  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        advanced:
          fields:
            overrides:
              fields:
                header.theme:
                  type: themeselect
                  label: PLUGINS.THEMER.THEME
                  help: PLUGINS.THEMER.THEME_HELP
                  '@config-default': "system.pages.theme"
                  toggleable: true
                  selectize:
                    create: true

                  validate:
                    type: string
